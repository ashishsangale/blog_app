<% blogs.forEach(function(blog){ %>
    <div>
        <h2><%= blog.title %> </h2>
        <img src="<%= blog.image %> ">
        <p><%= blog.body %> </p>
        <span><%= blog.created %> </span>
    </div> 
<% }) %> 